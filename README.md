# The-Native

  -->   npx @react-native-community/cli init TempRN

  --> installation of Navigation 

  --> Struchearing the Folder flow 


  --> Easing out the image , Style and text and reported importing problem
      By creating 
      Root.ts -- for Styles 
      index.ts -- for images 
  
  --> Creating a Login Screen and Home Screen with basic navigation 

  --> Flat list  static flat list 





  New :

  Strucheared API integration 
    --> Created a API folder and created a file for each API call
    --> Created a file for each API call and created a function to call the API and return the data
    --> Created a file for each API call and created a function to call the API and return the data and handle errors
    --> Created a file for each API call and created a function to call the API and return the data and handle errors and loading state
    --> Created a file for each API call and created a function to call the API and return the data and handle errors and loading state and used it in the Home Screen to display the data
    
   Handle Repeated code by creating components for reuse (Component Reusability)
   Redux concept 
   React Components
        ↓
     Actions
        ↓
     Redux Store
        ↓
      State
        ↓
React Components update


Concept	Purpose	When to Use
useState	Local component state	State only needed in one component
useReducer	Complex local state logic	Multiple related state updates
Context API	Share state across components	Avoid prop drilling
Redux / Redux Toolkit	Centralized global state	Large apps with complex shared state
Zustand	Lightweight global state	Simpler alternative to Redux
Jotai	Atomic state management	Fine-grained state updates
Recoil	Shared state with atom-based model	Complex state relationships
MobX	Observable state management	Reactive programming style

Login state managment based on user role

MCP? Model Context Protocol
